# CodeAtlas — Codebase Semantic Context

> Generated by CodeAtlas. Use this file as the entry point for LLM tools.
> Last updated: 2026-03-30T19:56:42.649Z

## Codebase Stats
- **Total files analyzed:** 30
- **Total symbols indexed:** 151
- **Dependency edges:** 69

## How to Use This Index

All context files are in `.codeatlas/context/`. Each source file has a corresponding
`.md` file describing its symbols in plain English.

- **`index.json`** — Full semantic index. Contains every symbol with plain-English descriptions.
- **`graph.json`** — File dependency graph. Shows import relationships.
- **`context/<path>.md`** — Per-file English descriptions.

## File Overview

### `src/`
- **`src/extension.ts`** — This file is the main entry point for the CodeAtlas extension, responsible for initializing and managing the extension's functionality, including setting up the UI, handling user interactions, and …

### `src/core/analyzer/`
- **`src/core/analyzer/fileAnalyzer.ts`** — This file contains the main logic for analyzing a single file or a batch of files using the LLM (Large Language Model). It provides functions for analyzing files individually or in batches, handlin…
- **`src/core/analyzer/graphBuilder.ts`** — This file is responsible for building a dependency graph of source files, including their symbols and import relationships. It uses a combination of ctags and regex-based parsing to extract symbols…
- **`src/core/analyzer/indexer.ts`** — This file, `indexer.ts`, is responsible for building a semantic index of a codebase, which includes a flat symbol index, a file index, and an inverted keyword index. It aggregates data from individ…

### `src/core/cache/`
- **`src/core/cache/contextCache.ts`** — Provides a cache for file contexts.

### `src/core/git/`
- **`src/core/git/gitService.ts`** — This file provides a set of Git-related utility functions for managing branches, diffs, and content hashing. It allows for retrieving branch information, computing content hashes, and capturing the…

### `src/core/stats/`
- **`src/core/stats/usageStats.ts`** — This file provides functionality for tracking and persisting usage statistics of language models (LLMs) across multiple workflow runs, including input/output tokens, request counts, and error rates.

### `src/core/workflow/`
- **`src/core/workflow/runner.ts`** — This file contains the core logic for the CodeAtlas workflow, which analyzes source files and builds a semantic index for code understanding and navigation.

### `src/llm/`
- **`src/llm/factory.ts`** — LLM factory function that creates an instance of a language model provider based on the user's configuration.
- **`src/llm/rateLimiter.ts`** — Provides a rate limiter for the LLM module.
- **`src/llm/types.ts`** — Defines types and interfaces for the LLM (Large Language Model) module.

### `src/llm/providers/`
- **`src/llm/providers/claude.ts`** — ClaudeProvider connects to the Anthropic API to generate text based on user input.
- **`src/llm/providers/gemini.ts`** — Provides a Gemini LLM provider.
- **`src/llm/providers/local.ts`** — LocalProvider is a class that connects to OpenAI-compatible local inference servers for text completion tasks, supporting early termination and cancellation.
- **`src/llm/providers/ollama.ts`** — OllamaProvider connects to a locally-running Ollama instance using its native NDJSON streaming API.
- **`src/llm/providers/openai.ts`** — Provides an OpenAI LLM provider.
- **`src/llm/providers/openaiCompat.ts`** — OpenAICompatProvider connects to a remote server that exposes the OpenAI /v1/chat/completions API.

### `src/server/`
- **`src/server/serverManager.ts`** — Manages a llama-server child process, providing a singleton instance for starting, stopping, and monitoring the server.

### `src/test/`
- **`src/test/index.ts`** — Runs tests for the extension using the test-electron module.

### `src/test/suite/`
- **`src/test/suite/fileAnalyzer.test.ts`** — _No overview available_
- **`src/test/suite/fileUtils.test.ts`** — Tests file utility functions.
- **`src/test/suite/graphBuilder.test.ts`** — This file contains unit tests for the GraphBuilder class, specifically for the extractSymbolsFromContent and extractImports methods. The tests cover various programming languages, including TypeScr…
- **`src/test/suite/index.ts`** — Runs tests for the extension using Mocha.
- **`src/test/suite/indexer.test.ts`** — This file contains unit tests for the indexer functionality, which is responsible for building a semantic index of code files. The tests cover various aspects of the indexer, including building the…
- **`src/test/suite/rateLimiter.test.ts`** — Tests the rate limiter class.
- **`src/test/suite/usageStats.test.ts`** — This file contains unit tests for the UsageTracker class and the getStatsDisplay function, which are used to track and display usage statistics for language models.

### `src/utils/`
- **`src/utils/fileUtils.ts`** — This file provides utility functions for file system operations, branch management, and workspace configuration. It exports constants, functions, and variables for working with the CodeAtlas direct…
- **`src/utils/logger.ts`** — Provides a logging utility for the application.

### `src/vscode/`
- **`src/vscode/activityBar.ts`** — CodeAtlasActivityProvider class that displays the CodeAtlas activity bar item in the VS Code activity bar.
- **`src/vscode/statusBar.ts`** — CodeAtlasStatusBar class that displays the CodeAtlas status bar item in the VS Code status bar.

## Key Symbols

Top-level exported symbols across the codebase:

- **`LLMMessage`** (interface) in `src/llm/types.ts` — Represents a message sent to or received from the LLM.
- **`LLMResponse`** (interface) in `src/llm/types.ts` — Represents the response from the LLM.
- **`ILLMProvider`** (interface) in `src/llm/types.ts` — Defines the interface for an LLM provider.
- **`LLMConfig`** (interface) in `src/llm/types.ts` — Represents the configuration for an LLM provider.
- **`LLMError`** (class) in `src/llm/types.ts` — Represents an error that occurred while using the LLM.
- **`RateLimitError`** (class) in `src/llm/types.ts` — Represents a rate limit error that occurred while using the LLM.
- **`Logger`** (class) in `src/utils/logger.ts` — Represents a logger that can output messages to the console.
- **`RateLimiter`** (class) in `src/llm/rateLimiter.ts` — Represents a rate limiter that tracks timestamps of recent requests.
- **`RateLimiter`** (class) in `src/test/suite/rateLimiter.test.ts` — Rate limiter class.
- **`sleep`** (function) in `src/llm/rateLimiter.ts` — Provides a function that sleeps for a specified amount of time.
- **`CacheEntry`** (interface) in `src/core/cache/contextCache.ts` — Represents a cache entry that contains a file context and its content hash.
- **`cacheFileContext`** (function) in `src/core/cache/contextCache.ts` — Caches a file context in the shared cache.
- **`getCachedFileContext`** (function) in `src/core/cache/contextCache.ts` — Retrieves a cached file context from the shared cache.
- **`hasCachedContext`** (function) in `src/core/cache/contextCache.ts` — Checks if a cached context exists for a given content hash.
- **`loadBranchState`** (function) in `src/core/cache/contextCache.ts` — Loads the branch state for a specific branch.
- **`saveBranchState`** (function) in `src/core/cache/contextCache.ts` — Saves the branch state for the active branch.
- **`updateBranchState`** (function) in `src/core/cache/contextCache.ts` — Updates the branch state after a workflow run.
- **`removeFromBranchState`** (function) in `src/core/cache/contextCache.ts` — Removes deleted files from the branch state.
- **`hasBranchIndex`** (function) in `src/core/cache/contextCache.ts` — Checks if a branch has been previously indexed.
- **`batchCacheContexts`** (function) in `src/core/cache/contextCache.ts` — Batch-caches multiple file contexts into the shared cache and updates the branch state.
- **`partitionByCacheHit`** (function) in `src/core/cache/contextCache.ts` — Partitions a list of files into cached and uncached files.
- **`OpenAIProvider`** (class) in `src/llm/providers/openai.ts` — Represents an OpenAI LLM provider.
- **`GeminiProvider`** (class) in `src/llm/providers/gemini.ts` — Represents a Gemini LLM provider.
- **`parseRetryDelay`** (function) in `src/llm/providers/gemini.ts` — Parses the retry delay from a 429 response.
- **`complete`** (function) in `src/llm/providers/claude.ts` — Generate text based on user input.
- **`complete`** (function) in `src/llm/providers/ollama.ts` — Generate text based on user input.
- **`complete`** (function) in `src/llm/providers/openaiCompat.ts` — Generate text based on user input.
- **`createLLMProvider`** (function) in `src/llm/factory.ts` — Create a language model provider instance.
- **`CodeAtlasStatusBar`** (class) in `src/vscode/statusBar.ts` — Display the CodeAtlas status bar item.
- **`CodeAtlasActivityProvider`** (class) in `src/vscode/activityBar.ts` — Display the CodeAtlas activity bar item.
- **`main`** (function) in `src/test/index.ts` — Entrypoint for running tests.
- **`sanitizeBranchName`** (function) in `src/test/suite/fileUtils.test.ts` — Sanitizes a branch name by replacing forward slashes with double dashes.
- **`sanitizeBranchName`** (function) in `src/utils/fileUtils.ts` — Sanitize a git branch name for use as a filesystem directory name
- **`chunkText`** (function) in `src/test/suite/fileUtils.test.ts` — Chunks text into smaller pieces based on a maximum character limit.
- **`chunkText`** (function) in `src/utils/fileUtils.ts` — Chunk text into smaller pieces
- **`detectLanguage`** (function) in `src/test/suite/fileUtils.test.ts` — Detects the language of a file based on its extension.
- **`detectLanguage`** (function) in `src/utils/fileUtils.ts` — Detect the language of a file
- **`getCodeAtlasDirForRoot`** (function) in `src/test/suite/fileUtils.test.ts` — Returns the path to the .codeatlas directory for a given root directory.
- **`getCodeAtlasDirForRoot`** (function) in `src/utils/fileUtils.ts` — Get the CodeAtlas directory path for a given workspace root
- **`run`** (function) in `src/test/suite/index.ts` — Runs tests using Mocha.
- **`getWorkspaceRoot`** (function) in `src/utils/fileUtils.ts` — Get the workspace root directory
- **`getCodeAtlasDir`** (function) in `src/utils/fileUtils.ts` — Get the CodeAtlas directory path
- **`getBranchDir`** (function) in `src/utils/fileUtils.ts` — Get the per-branch directory under `.codeatlas/branches/{sanitized-branch}/`
- **`getSharedCacheDir`** (function) in `src/utils/fileUtils.ts` — Get the shared content-hash cache directory (`.codeatlas/cache/`)
- **`setActiveBranch`** (function) in `src/utils/fileUtils.ts` — Set the active branch
- **`getActiveBranch`** (function) in `src/utils/fileUtils.ts` — Get the currently active branch name
- **`getActiveBranchDir`** (function) in `src/utils/fileUtils.ts` — Get the directory for the currently active branch
- **`getContextDir`** (function) in `src/utils/fileUtils.ts` — Get the context directory
- **`getGraphFilePath`** (function) in `src/utils/fileUtils.ts` — Get the graph file path
- **`getIndexFilePath`** (function) in `src/utils/fileUtils.ts` — Get the index file path