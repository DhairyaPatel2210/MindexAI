# CodeAtlas — Codebase Semantic Context

> Generated by CodeAtlas. Use this file as the entry point for LLM tools.
> Last updated: 2026-03-31T02:20:52.297Z

## Codebase Stats
- **Total files analyzed:** 31
- **Total symbols indexed:** 199
- **Dependency edges:** 69

## How to Use This Index

All context files are in `.codeatlas/context/`. Each source file has a corresponding
`.md` file describing its symbols in plain English.

- **`index.json`** — Full semantic index. Contains every symbol with plain-English descriptions.
- **`graph.json`** — File dependency graph. Shows import relationships.
- **`context/<path>.md`** — Per-file English descriptions.

## File Overview

### `src/`
- **`src/extension.ts`** — This file is the main entry point for the CodeAtlas extension, responsible for initializing and managing the extension's functionality, including status bar, main panel, activity bar, and auto-upda…

### `src/core/analyzer/`
- **`src/core/analyzer/fileAnalyzer.ts`** — Analyzes a single file or a batch of small files using a Large Language Model (LLM) to extract semantic documentation.
- **`src/core/analyzer/graphBuilder.ts`** — This file is responsible for building a dependency graph of source files, including their symbols, imports, and relationships.
- **`src/core/analyzer/indexer.ts`** — This file is responsible for building a semantic index of a codebase, which includes a flat symbol index, a file index, and an inverted keyword index. The index is built by analyzing file contexts …

### `src/core/cache/`
- **`src/core/cache/contextCache.ts`** — Defines a cache for file contexts.

### `src/core/git/`
- **`src/core/git/gitService.ts`** — This file provides a set of functions for interacting with a Git repository, including getting branch information, diffing files, and computing content hashes.

### `src/core/stats/`
- **`src/core/stats/usageStats.ts`** — This file provides functionality for tracking and persisting usage statistics of language models (LLMs) across multiple workflow runs, including input/output token counts, request counts, and error…

### `src/core/workflow/`
- **`src/core/workflow/runner.ts`** — This file contains the core logic for running workflows in the Codeatlas project, including full analysis, incremental updates, and single-file analysis. It utilizes dependency graphs, caching, and…

### `src/llm/`
- **`src/llm/factory.ts`** — LLM factory that creates instances of LLM providers based on configuration.
- **`src/llm/rateLimiter.ts`** — Defines a rate limiter utility for the LLM module.
- **`src/llm/types.ts`** — Defines types and interfaces for the LLM (Large Language Model) module.

### `src/llm/providers/`
- **`src/llm/providers/claude.ts`** — ClaudeProvider is an LLM provider that connects to the Anthropic API.
- **`src/llm/providers/gemini.ts`** — Defines a Gemini LLM provider.
- **`src/llm/providers/local.ts`** — LocalProvider is a class that connects to OpenAI-compatible local inference servers, providing a way to interact with these servers through a unified interface.
- **`src/llm/providers/ollama.ts`** — OllamaProvider is an LLM provider that connects to a locally-running Ollama instance.
- **`src/llm/providers/openai.ts`** — Defines an OpenAI LLM provider.
- **`src/llm/providers/openaiCompat.ts`** — OpenAICompatProvider is an LLM provider that connects to a remote server that exposes the OpenAI /v1/chat/completions API.

### `src/server/`
- **`src/server/serverManager.ts`** — Manages a llama-server (llama.cpp) child process, providing a singleton instance for starting, stopping, and monitoring the server.

### `src/test/`
- **`src/test/index.ts`** — Test suite for CodeAtlas.

### `src/test/suite/`
- **`src/test/suite/fileAnalyzer.test.ts`** — This file contains tests for the fileAnalyzer module, which provides functions for extracting JSON data, normalizing symbol data, rendering context markdown, and partitioning files for batching.
- **`src/test/suite/fileUtils.test.ts`** — Unit tests for file utilities
- **`src/test/suite/graphBuilder.test.ts`** — This file contains unit tests for the GraphBuilder class, which is responsible for extracting symbols and imports from source code files. The tests cover various programming languages, including Ty…
- **`src/test/suite/index.ts`** — Entry point for running tests
- **`src/test/suite/indexer.test.ts`** — This file contains unit tests for the indexer functionality, which is responsible for building and populating a semantic index of code. The tests cover various scenarios, including building the ind…
- **`src/test/suite/rateLimiter.test.ts`** — Unit tests for rate limiter
- **`src/test/suite/usageStats.test.ts`** — This file contains unit tests for the usage statistics functionality, specifically for the UsageTracker class and the getStatsDisplay function.

### `src/utils/`
- **`src/utils/fileUtils.ts`** — This file provides utility functions for file system operations, path manipulation, and workspace management, primarily for use in a code analysis and AI code editor context.
- **`src/utils/logger.ts`** — Defines a logger utility for the application.

### `src/vscode/`
- **`src/vscode/activityBar.ts`** — This file provides the implementation for the Code Atlas activity bar in Visual Studio Code, which displays a tree view of available actions and statistics.
- **`src/vscode/statusBar.ts`** — CodeAtlasStatusBar is a VS Code status bar item that displays the CodeAtlas status.

### `src/vscode/panel/`
- **`src/vscode/panel/mainPanel.ts`** — This file implements the main panel of the CodeAtlas extension for Visual Studio Code, responsible for displaying and managing the setup and configuration of language models.

## Key Symbols

Top-level exported symbols across the codebase:

- **`analyzeFile`** (function) in `src/core/analyzer/fileAnalyzer.ts` — Analyzes a single file using the LLM.
- **`analyzeFileBatch`** (function) in `src/core/analyzer/fileAnalyzer.ts` — Analyzes a batch of small files using the LLM.
- **`partitionForBatching`** (function) in `src/core/analyzer/fileAnalyzer.ts` — Partitions a list of file nodes into batches of small files and a list of large files.
- **`partitionForBatching`** (function) in `src/test/suite/fileAnalyzer.test.ts` — Partitions files into batches and large files, based on size thresholds.
- **`buildDependencyGraph`** (function) in `src/core/analyzer/graphBuilder.ts` — Builds a dependency graph of source files, including their symbols, imports, and relationships.
- **`isCtagsAvailable`** (function) in `src/core/analyzer/graphBuilder.ts` — Checks if the ctags command is available on the system.
- **`extractSymbolsWithCtags`** (function) in `src/core/analyzer/graphBuilder.ts` — Extracts symbols from source files using the ctags command.
- **`extractSymbolsWithRegex`** (function) in `src/core/analyzer/graphBuilder.ts` — Extracts symbols from source files using regular expressions.
- **`buildImportGraph`** (function) in `src/core/analyzer/graphBuilder.ts` — Builds an import graph of source files.
- **`extractImports`** (function) in `src/core/analyzer/graphBuilder.ts` — Extracts import statements from source files.
- **`extractImports`** (function) in `src/test/suite/graphBuilder.test.ts` — Extracts imports from a given source code content.
- **`normalizeKind`** (function) in `src/core/analyzer/graphBuilder.ts` — Normalizes the kind of a symbol.
- **`getLanguageFromPath`** (function) in `src/core/analyzer/graphBuilder.ts` — Determines the language of a source file based on its file extension.
- **`buildIndex`** (function) in `src/core/analyzer/indexer.ts` — Builds a semantic index of a codebase
- **`buildIndex`** (function) in `src/test/suite/indexer.test.ts` — Builds and populates a semantic index of code
- **`persistIndex`** (function) in `src/core/analyzer/indexer.ts` — Persists a semantic index to disk in JSON format
- **`persistContextOverview`** (function) in `src/core/analyzer/indexer.ts` — Persists a context overview to disk in Markdown format
- **`inlineOverview`** (function) in `src/core/analyzer/indexer.ts` — Formats an overview string for inline use in a Markdown list item
- **`CacheEntry`** (interface) in `src/core/cache/contextCache.ts` — Represents a cache entry.
- **`cacheFileContext`** (function) in `src/core/cache/contextCache.ts` — Caches a file context.
- **`getCachedFileContext`** (function) in `src/core/cache/contextCache.ts` — Gets a cached file context.
- **`hasCachedContext`** (function) in `src/core/cache/contextCache.ts` — Checks if a cached context exists.
- **`getCurrentBranch`** (function) in `src/core/git/gitService.ts` — Returns the current branch name, or HEAD commit hash if detached, or 'default' for empty repos.
- **`getHeadCommit`** (function) in `src/core/git/gitService.ts` — Returns the commit hash that HEAD points to.
- **`getChangedFilesBetweenRefs`** (function) in `src/core/git/gitService.ts` — Get files that differ between two refs (branches/commits).
- **`getUncommittedChanges`** (function) in `src/core/git/gitService.ts` — Get uncommitted changes (both staged and unstaged) relative to HEAD.
- **`getAllChangedFiles`** (function) in `src/core/git/gitService.ts` — Get ALL files that have changed relative to the last indexed state.
- **`getFileContentHash`** (function) in `src/core/git/gitService.ts` — Compute a content hash for a file using the same algorithm as git (SHA-1 of blob).
- **`getFileHashAtRef`** (function) in `src/core/git/gitService.ts` — Get the git blob hash for a file at a specific ref (commit/branch).
- **`captureGitState`** (function) in `src/core/git/gitService.ts` — Capture the current state of the Git repository.
- **`ProviderStats`** (interface) in `src/core/stats/usageStats.ts` — Represents statistics for a single LLM provider, including input/output token counts, request counts, and error rates.
- **`RunRecord`** (interface) in `src/core/stats/usageStats.ts` — Represents a single workflow run, including timestamp, provider, input/output token counts, and error rates.
- **`UsageStatsData`** (interface) in `src/core/stats/usageStats.ts` — Represents the overall usage statistics, including provider statistics, global statistics, and run history.
- **`StatsDisplay`** (interface) in `src/core/stats/usageStats.ts` — Represents a display of usage statistics, including provider statistics and global statistics.
- **`UsageTracker`** (class) in `src/core/stats/usageStats.ts` — A lightweight accumulator that tracks usage statistics for a single workflow run.
- **`UsageTracker`** (class) in `src/test/suite/usageStats.test.ts` — Tracks usage statistics for a workflow, including input tokens, output tokens, unsplit tokens, request count, and total duration.
- **`getStatsDisplay`** (function) in `src/core/stats/usageStats.ts` — Returns a display of usage statistics for a given usage statistics data.
- **`getStatsDisplay`** (function) in `src/test/suite/usageStats.test.ts` — Computes a displayable representation of usage statistics from a UsageStatsData object.
- **`loadUsageStats`** (function) in `src/core/stats/usageStats.ts` — Loads persisted usage statistics from file.
- **`saveUsageStats`** (function) in `src/core/stats/usageStats.ts` — Saves usage statistics to file.
- **`persistWorkflowUsage`** (function) in `src/core/stats/usageStats.ts` — Merges a completed workflow run's tracker into the persisted stats.
- **`resetUsageStats`** (function) in `src/core/stats/usageStats.ts` — Resets all persisted usage statistics.
- **`runFullWorkflow`** (function) in `src/core/workflow/runner.ts` — Runs a full analysis workflow on the current branch, including collecting source files, building dependency graphs, and analyzing files with the LLM.
- **`runIncrementalUpdate`** (function) in `src/core/workflow/runner.ts` — Runs an incremental update workflow on the current branch, including detecting changes, rebuilding dependency graphs, and analyzing updated files with the LLM.
- **`runSingleFileAnalysis`** (function) in `src/core/workflow/runner.ts` — Analyzes a single file with the LLM, including checking the shared cache and caching the result.
- **`activate`** (function) in `src/extension.ts` — Initialize and activate the CodeAtlas extension
- **`deactivate`** (function) in `src/extension.ts` — Deactivate and clean up the CodeAtlas extension
- **`setupAutoUpdate`** (function) in `src/extension.ts` — Set up auto-update features for the CodeAtlas extension
- **`disposeAutoUpdate`** (function) in `src/extension.ts` — Dispose of auto-update resources
- **`disposeAutoUpdate`** (function) in `src/extension.ts` — Dispose of auto-update resources, including clearing intervals and file watchers.